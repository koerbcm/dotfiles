#!/usr/bin/sh

# Alert user that there are modifications to dotfiles
# CHANGES=$(git -C $DOTFILES diff --name-only | wc -l | tr -d '[:space:]')

# if [[ ${CHANGES} != 0 ]]; then
#   warn "\n**Your dotfiles contain ${CHANGES} uncommitted change(s)**"
# fi
